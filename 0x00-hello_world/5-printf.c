#include <stdio.h>

/**
 * main - entry point
 * Description: A c program that prints with printf function
 * Return: always 0 (success)
*/

int main(void)
{
	pfintf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
